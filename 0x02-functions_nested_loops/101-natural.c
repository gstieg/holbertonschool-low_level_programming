#include <stdio.h>

/**
 * main - increase and decrease
 *
 * Return: 0
 *
 */
int main(void)
{
	int c, d;

	for (c = 0; c < 1024; c++)
	{
		if (c % 3 == 0 || c % 5 == 0)
			d = d + a;
	}
	printf("%d\n", d);
	return (0);
}
